import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    position: 'relative',
  },
  btnToggle: {
    position: 'absolute',
    top: '50%',
    right: 20,
    marginTop: -10,
  },
});
